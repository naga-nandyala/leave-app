# Environment Configuration for Leave App
# Copy this file to .env and update the values as needed

# Storage Configuration
# Set to 'local' for local file storage or 'azure' for Azure Blob Storage
STORAGE_TYPE=local

# Azure Storage Configuration (only needed when STORAGE_TYPE=azure)
# AZURE_STORAGE_ACCOUNT_NAME=yourstorageaccountname
# AZURE_STORAGE_CONTAINER_NAME=leaveapp-data

# Application Configuration
SECRET_KEY=your-secret-key-change-this-in-production

# Authentication Configuration for Azure (when using STORAGE_TYPE=azure)
# The app will use DefaultAzureCredential which supports:
# 1. Managed Identity (recommended for Azure-hosted apps)
# 2. Azure CLI credentials (for local development)
# 3. Environment variables (AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID)
# 4. Visual Studio / VS Code credentials

# For local development with Azure Storage:
# 1. Install Azure CLI: https://docs.microsoft.com/en-us/cli/azure/install-azure-cli
# 2. Run: az login
# 3. Set STORAGE_TYPE=azure and AZURE_STORAGE_ACCOUNT_NAME

# For production deployment:
# 1. Use Managed Identity (preferred)
# 2. Or set AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID
